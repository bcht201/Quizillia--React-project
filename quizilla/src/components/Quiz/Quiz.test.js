import React from 'react';
import {
    shallow, 
    mount
} from 'enzyme';
import Quiz from './Quiz'

describe('Quiz.js', () => {
    let wrapper;

    beforeEach(() => {
        wrapper= shallow(<Quiz />);
    })
    
        
})

